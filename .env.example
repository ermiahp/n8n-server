# Example environment variables for n8n-server
# Copy this file to .env and update with your actual values

# Docker Compose configuration
COMPOSE_PROJECT_NAME=n8n-server

# Portainer configuration
PORTAINER_HTTPS_PORT=9443
PORTAINER_HTTP_PORT=8000

# N8N configuration
# N8N_HOST should be your domain name when using nginx proxy manager
N8N_HOST=localhost
N8N_PORT=5678
N8N_PROTOCOL=http

# Webhook URL - Set this to your external domain for webhooks to work properly
# Example: https://n8n.yourdomain.com/ when using nginx proxy manager with SSL
N8N_WEBHOOK_URL=http://localhost:5678/

# Timezone configuration
N8N_GENERIC_TIMEZONE=UTC
N8N_TZ=UTC

# Important: Change this to a random string for security!
N8N_ENCRYPTION_KEY=change-this-to-a-random-string

# Optional: Basic authentication (uncomment to enable)
# N8N_BASIC_AUTH_USER=admin
# N8N_BASIC_AUTH_PASSWORD=your-secure-password

# Optional: Database configuration (if using external database)
# N8N_DB_NAME=n8n
# N8N_DB_USER=n8n
# N8N_DB_PASSWORD=your-db-password

# Optional: Execution settings
N8N_EXECUTIONS_PROCESS=main
N8N_EXECUTIONS_MODE=regular

# Add any additional environment variables here
